{"id":28,"module":"5. Dynamic Programming","type":"challenge","number":84,"name":"counting change","slug":"counting-change","verbose":"Count the number of different ways to make change using coins.","timeEstimate":60,"premium":false,"tags":["dynamic programming"],"environments":{"node":{"prompt":"## counting change\n\nWrite a function, _countingChange_, that takes in an amount and an array of coins. The function should\nreturn the number of different ways it is possible to make change for the given amount using the coins.\n\nYou may reuse a coin as many times as necessary.\n\n```plaintext\nFor example,\n\ncountingChange(4, [1,2,3]) -> 4\n\nThere are four different ways to make an amount of 4:\n\n1. 1 + 1 + 1 + 1\n2. 1 + 1 + 2\n3. 1 + 3\n4. 2 + 2\n```\n\n#### test_00:\n\n```js\ncountingChange(4, [1, 2, 3]); // -> 4\n```\n\n#### test_01:\n\n```js\ncountingChange(8, [1, 2, 3]); // -> 10\n```\n\n#### test_02:\n\n```js\ncountingChange(24, [5, 7, 3]); // -> 5\n```\n\n#### test_03:\n\n```js\ncountingChange(13, [2, 6, 12, 10]); // -> 0\n```\n\n#### test_04:\n\n```js\ncountingChange(512, [1, 5, 10, 25]); // -> 20119\n```\n\n#### test_05:\n\n```js\ncountingChange(1000, [1, 5, 10, 25]); // -> 142511\n```\n\n#### test_06:\n\n```js\ncountingChange(240, [1, 2, 3, 4, 5, 6, 7, 8, 9]); // -> 1525987916\n```\n","stub":"const countingChange = (amount, coins) => {\n  // todo\n};\n\nmodule.exports = {\n  countingChange,\n};\n","testHarness":"const { assert } = require(\"/var/task/node_modules/chai\");\n\nclass TestHarness {\n  constructor(source) {\n    this.source = source;\n  }\n\n  test00() {\n    const actual = this.source.countingChange(4, [1, 2, 3]);\n    const expect = 4;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n\n  test01() {\n    const actual = this.source.countingChange(8, [1, 2, 3]);\n    const expect = 10;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n\n  test02() {\n    const actual = this.source.countingChange(24, [5, 7, 3]);\n    const expect = 5;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n\n  test03() {\n    const actual = this.source.countingChange(13, [2, 6, 12, 10]);\n    const expect = 0;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n\n  test04() {\n    const actual = this.source.countingChange(512, [1, 5, 10, 25]);\n    const expect = 20119;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n\n  test05() {\n    const actual = this.source.countingChange(1000, [1, 5, 10, 25]);\n    const expect = 142511;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n\n  test06() {\n    const actual = this.source.countingChange(240, [1, 2, 3, 4, 5, 6, 7, 8, 9]);\n    const expect = 1525987916;\n    try {\n      assert.isNumber(actual);\n      assert.strictEqual(expect, actual);\n      return [true, expect, actual];\n    } catch (AssertionError) {\n      return [false, expect, actual];\n    }\n  }\n}\n\nmodule.exports = TestHarness;\n","solutions":"## solution\n\n#### dynamic programming with memoization\n\n```js\nconst countingChange = (amount, coins, i = 0, memo = {}) => {\n  const key = amount + ',' + i;\n  if (key in memo) return memo[key];\n  \n  if (amount === 0) return 1;\n  if (i === coins.length) return 0;\n    \n  const coin = coins[i];\n  \n  let count = 0;\n  for (let qty = 0; (qty * coin) <= amount; qty += 1) {\n    const remainder = amount - (coin * qty);\n    count += countingChange(remainder, coins, i + 1, memo);\n  }\n  \n  memo[key] = count;\n  return count;\n};\n```\n\n- a = amount\n- c = # coins\n- Time: O(a*c)\n- Space: O(a*c)\n","videos":{"approach":"https://player.vimeo.com/video/544849181","walkthrough":"https://player.vimeo.com/video/545314538"}},"python3":{"prompt":"## counting change\n\nWrite a function, *counting_change*, that takes in an amount and a list of coins. The function should\nreturn the number of different ways it is possible to make change for the given amount using the coins.\n\nYou may reuse a coin as many times as necessary.\n\n```plaintext\nFor example,\n\ncounting_change(4, [1,2,3]) -> 4\n\nThere are four different ways to make an amount of 4:\n\n1. 1 + 1 + 1 + 1\n2. 1 + 1 + 2\n3. 1 + 3\n4. 2 + 2\n```\n\n#### test_00:\n\n```python\ncounting_change(4, [1, 2, 3]) # -> 4\n```\n\n#### test_01:\n\n```python\ncounting_change(8, [1, 2, 3]) # -> 10\n```\n\n#### test_02:\n\n```python\ncounting_change(24, [5, 7, 3]) # -> 5\n```\n\n#### test_03:\n\n```python\ncounting_change(13, [2, 6, 12, 10]) # -> 0\n```\n\n#### test_04:\n\n```python\ncounting_change(512, [1, 5, 10, 25]) # -> 20119\n```\n\n#### test_05:\n\n```python\ncounting_change(1000, [1, 5, 10, 25]) # -> 142511\n```\n\n#### test_06:\n\n```python\ncounting_change(240, [1, 2, 3, 4, 5, 6, 7, 8, 9]) # -> 1525987916\n```\n","stub":"def counting_change(amount, coins):\n  pass # todo\n","testHarness":"import unittest\n\nclass TestHarness(unittest.TestCase):\n  def __init__(self, source):\n    super().__init__()\n    self.source = source\n\n  def test00(self):\n    actual = self.source.counting_change(4, [1, 2, 3]) \n    expect = 4\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n\n  def test01(self):\n    actual = self.source.counting_change(8, [1, 2, 3])\n    expect = 10\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n\n  def test02(self):\n    actual = self.source.counting_change(24, [5, 7, 3])\n    expect = 5\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n\n  def test03(self):\n    actual = self.source.counting_change(13, [2, 6, 12, 10])\n    expect = 0\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n\n  def test04(self):\n    actual = self.source.counting_change(512, [1, 5, 10, 25]) \n    expect = 20119\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n\n  def test05(self):\n    actual = self.source.counting_change(1000, [1, 5, 10, 25])\n    expect = 142511\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n\n  def test06(self):\n    actual = self.source.counting_change(240, [1, 2, 3, 4, 5, 6, 7, 8, 9])\n    expect = 1525987916\n    try:\n      self.assertIsInstance(actual, int)\n      self.assertEqual(expect, actual)\n      return (True, expect, actual)\n    except AssertionError:\n      return (False, expect, actual)\n","solutions":"## solution\n\n#### dynamic programming with memoization\n\n```python\ndef counting_change(amount, coins):\n  return _counting_change(amount, coins, 0, {})\n\ndef _counting_change(amount, coins, i, memo):\n  key = (amount, i)\n  if key in memo:\n    return memo[key]\n  \n  if amount == 0:\n    return 1\n  \n  if i == len(coins):\n    return 0\n  \n  coin = coins[i]\n  \n  total_count = 0\n  for qty in range(0, (amount // coin) + 1):\n    remainder = amount - (qty * coin)\n    total_count += _counting_change(remainder, coins, i + 1, memo)\n    \n  memo[key] = total_count\n  return total_count\n\n```\n\n- a = amount\n- c = # coins\n- Time: O(a*c)\n- Space: O(a*c)","videos":{"approach":"https://player.vimeo.com/video/544849181","walkthrough":"https://player.vimeo.com/video/545314320"}},"cpp":{"prompt":"## counting change\n\nWrite a function, _countingChange_, that takes in an amount and a vector of coins. The function should\nreturn the number of different ways it is possible to make change for the given amount using the coins.\n\nYou may reuse a coin as many times as necessary.\n\n```plaintext\nFor example,\n\ncountingChange(4, std::vector<int> {1,2,3}) -> 4\n\nThere are four different ways to make an amount of 4:\n\n1. 1 + 1 + 1 + 1\n2. 1 + 1 + 2\n3. 1 + 3\n4. 2 + 2\n```\n\n#### test_00:\n\n```cpp\ncountingChange(4, std::vector<int> {1, 2, 3}); // -> 4\n```\n\n#### test_01:\n\n```cpp\ncountingChange(8, std::vector<int> {1, 2, 3}); // -> 10\n```\n\n#### test_02:\n\n```cpp\ncountingChange(24, std::vector<int> {5, 7, 3}); // -> 5\n```\n\n#### test_03:\n\n```cpp\ncountingChange(13, std::vector<int> {2, 6, 12, 10}); // -> 0\n```\n\n#### test_04:\n\n```cpp\ncountingChange(512, std::vector<int> {1, 5, 10, 25}); // -> 20119\n```\n\n#### test_05:\n\n```cpp\ncountingChange(1000, std::vector<int> {1, 5, 10, 25}); // -> 142511\n```\n\n#### test_06:\n\n```cpp\ncountingChange(240, std::vector<int> {1, 2, 3, 4, 5, 6, 7, 8, 9}); // -> 1525987916\n```\n","stub":"#include <vector>\n\nint countingChange(int amount, std::vector<int> coins) {\n  // todo\n  return;\n}\n\n\nvoid run() {\n  // this function behaves as `main()` for the 'run' command\n  // you may sandbox in this function, but should not remove it\n}","testHarness":"#include \"source.cpp\"\n\n#include <functional>\n#include <map>\n#include <vector>\n#include <string>\n#include <tuple>\n#include <gtest/gtest.h>\n\nusing result = std::tuple<bool, int, int>;\n\nclass TestHarness {\n  private:\n    static result test00() {\n      int actual = countingChange(4, std::vector<int> {1, 2, 3});\n      int expect = 4;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n    static result test01() {\n      int actual = countingChange(8, std::vector<int> {1, 2, 3});\n      int expect = 10;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n    static result test02() {\n      int actual = countingChange(24, std::vector<int> {5, 7, 3});;\n      int expect = 5;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n    static result test03() {\n      int actual = countingChange(13, std::vector<int> {2, 6, 12, 10});\n      int expect = 0;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n    static result test04() {\n      int actual = countingChange(512, std::vector<int> {1, 5, 10, 25});\n      int expect = 20119;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n    static result test05() {\n      int actual = countingChange(1000, std::vector<int> {1, 5, 10, 25});\n      int expect = 142511;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n    static result test06() {\n      int actual = countingChange(240, std::vector<int> {1, 2, 3, 4, 5, 6, 7, 8, 9});\n      int expect = 1525987916;\n      result res = std::make_tuple(false, expect, actual);\n      [&res, expect, actual]() {\n        ASSERT_EQ(expect, actual);\n        res = std::make_tuple(true, expect, actual);\n      }();\n      return res;\n    }\n\n  public:\n    static std::map< std::string, std::function< result()> > test;\n};\n\nstd::map< std::string, std::function< result()> > TestHarness::test { \n  { \"00\", TestHarness::test00 },\n  { \"01\", TestHarness::test01 },\n  { \"02\", TestHarness::test02 },\n  { \"03\", TestHarness::test03 },\n  { \"04\", TestHarness::test04 },\n  { \"05\", TestHarness::test05 },\n  { \"06\", TestHarness::test06 }\n};","solutions":"## solution\n\n#### dynamic programming with memoization\n\n```cpp\n#include <vector>\n#include <unordered_map>\n#include <string>\n\nint countingChange(int amount, std::vector<int> coins, int i, std::unordered_map<std::string, int> &memo) {\n  std::string key = std::to_string(amount) + \",\" + std::to_string(i);\n  if (memo.count(key) > 0) {\n    return memo[key];\n  }\n  \n  if (amount == 0) {\n    return 1;\n  }\n  \n  if (i == coins.size()) {\n    return 0;\n  }\n  \n  int total = 0;\n  int coin = coins[i];\n  for (int qty = 0; (qty * coin) <= amount; qty += 1) {\n    total += countingChange(amount - (qty * coin), coins, i + 1, memo);\n  }\n  memo[key] = total;\n  return total;\n}\n\nint countingChange(int amount, std::vector<int> coins) {\n  std::unordered_map<std::string, int> memo;\n  return countingChange(amount, coins, 0, memo);\n}\n```\n\n- a = amount\n- c = # coins\n- Time: O(a*c)\n- Space: O(a*c)\n","videos":{"approach":"https://player.vimeo.com/video/544849181","walkthrough":"https://player.vimeo.com/video/589074210"}},"java":{}},"numberTests":7,"completed":false,"favorited":false,"previousProblem":{"type":"challenge","number":83,"name":"summing squares","slug":"summing-squares","verbose":"Find the minimum number of squares that add to a target number.","premium":false},"nextProblem":{"type":"challenge","number":85,"name":"array stepper","slug":"array-stepper","verbose":"Check if it is possible to step through an array from beginning to end.","premium":false}}
