# How to Use

- Before doing anything you have to login to Structy and get your authorization token.
- To get the question list (`question-urls-list.json`) you'll manually copy the JSON from the response once in the site.
- Now all you have to do is replace the current one in `./data` with the new JSON and run `process-question-list.ps1` after replacing the token argument with your current one.