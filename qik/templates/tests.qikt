// @{folderName}
// @{fileName}.js
// @{fileName}.spec.js
// @{fileName}.md

/* *************************************************************************************************
Problem:            @{title}
Difficulty:         @{difficulty}
Approach:           @{approach}
Leet No.:           @{leetNumber}
Leet Url:           @{leetUrl}
Efficiency:
    Time (Optimized): @{optimizedTimeComplexity}
    Space (Optmized): @{optimizedSpaceComplexity} 
************************************************************************************************* */

describe("@{name}", () => {
    const { @{naiveFuncName}, @{optimizedFuncName} } = require("./@{fileName}.js");
    beforeEach(() => {});

    describe("@{naiveFuncName} when called", () => {
        it(`should...`, () => {
            expect(@{naiveFuncName}(@{argList})).toEqual(@{expectedOutput});
        });
    });

    describe("@{optimizedFuncName} when called", () => {
        it(`should...`, () => {
            expect(@{optimizedFuncName}(@{argList})).toEqual(@{expectedOutput});
        });
    });
});

/* *************************************************************************************************
NimbleText

expect(@{naiveFuncName}(@{nimbleArgList})).toEqual(@{nimbleExpected});


expect(@{optimizedFuncName}(@{nimbleArgList})).toEqual(@{nimbleExpected});

************************************************************************************************* */
